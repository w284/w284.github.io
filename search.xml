<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>Carl Jung Mind-Map</title>
      <link href="/winnie/2d6afe5d.html"/>
      <url>/winnie/2d6afe5d.html</url>
      
        <content type="html"><![CDATA[<div class="row">    <embed src="/pictures/Jung.pdf" width="100%" height="550" type="application/pdf"></div>]]></content>
      
      
      <categories>
          
          <category> Psychology </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Caril Jung </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>CS246 Notes</title>
      <link href="/winnie/bda9ad51.html"/>
      <url>/winnie/bda9ad51.html</url>
      
        <content type="html"><![CDATA[<h1 id="9-5"><a href="#9-5" class="headerlink" title="9.5"></a>9.5</h1><p>We’ll look at OOP from three perspectives:</p><ul><li>Programmer’s perspective: how to structure program correctly &amp; how to lower the risk of bugs</li><li>The compiler’s perspective: what does constructors actually mean &amp; what must the compiler do to support them</li><li>The designer’s perspective How to use OOP tools to build systems(polymorphism, inheritance, encapsulation)</li></ul><h2 id="Intro-to-C"><a href="#Intro-to-C" class="headerlink" title="Intro to C++"></a><strong>Intro to C++</strong></h2><p>Hello world in C:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line">int main()&#123;</span><br><span class="line">printf(&quot;Hello world&quot;);</span><br><span class="line">return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Hello world in C++:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">import&lt;iostream&gt;;</span><br><span class="line">using namespace std;</span><br><span class="line">int main()&#123;</span><br><span class="line">cout &lt;&lt; &quot;Hello world&quot; &lt;&lt; end;</span><br><span class="line">return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Notes: </p><ul><li><p><code>import</code>, like <code>#include</code>, brings name and definition from other files, libraries, or modules into our code</p></li><li><p><code>import</code>, was C++ new module system</p></li><li><p><code>#include&lt;iostream&gt;</code> works too, but module system is preferred</p><ul><li><code>#include</code> is replaced by the contents of the header directly, while <code>#import</code> is only replaced by the contents of the header the first time that header is imported*</li></ul></li><li><p>function<code> main()</code> MUST  return <code>int</code>, <code>void main()&#123;&#125;</code> is not valid in C++</p></li><li><p>return statement return a status code to OS ($?: check last executed program’s status code)</p><ul><li>can be omitted from main, default to return 0</li></ul></li><li><p><code>cout</code> and <code>&lt;&lt;</code> are how you wish to stdout</p><ul><li>this is the preferred way to do I&#x2F;O in c++</li><li><code>stdio.h</code> and <code>printf</code> is still available in c++</li></ul></li><li><p><code>using namespace std</code></p><ul><li>called a using directive</li><li>c++ organizes names into namespaces</li><li>without <code>using namespaces std</code> , you need to write <code>std:: cout &lt;&lt; &quot;Hello world &quot;&lt;&lt; std::endl&quot;</code> </li><li><code>std:: </code></li></ul></li></ul><h2 id="Input-Output"><a href="#Input-Output" class="headerlink" title="Input&#x2F;Output"></a>Input&#x2F;Output</h2><p>c++ comes with 3 built-in I&#x2F;O streams:</p><ul><li><code>cout</code>: for writing to stdout</li><li><code>cerr</code>: for writing to stderr (never buffered)</li><li><code>cin</code>: for reading from stdin</li></ul><p>Built-in Operators:</p><ul><li><code>&lt;&lt; put to</code>, insertion operator(output)</li><li><code>&gt;&gt; get from</code> extraction operator(input)</li><li><code>cout &lt;&lt; __ &lt;&lt; __ &lt;&lt; __ &lt;&lt; endl;</code><ul><li>dates, expressions, literals</li><li><code>endl</code> outputs an end of line, char and flushes the output buffer</li></ul></li><li><code>cout &lt;&lt; x</code> operators “point” in the direction of data flow</li><li><code>cerr &lt;&lt; x</code></li><li><code>cin &gt;&gt; x</code> extract a value from stdin and put it into x</li></ul><p>Example: read 2 <code>int</code> and print their sum</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">import &lt;iostream&gt;;</span><br><span class="line">using namespace std;</span><br><span class="line">int mian()&#123;</span><br><span class="line">int x, y;</span><br><span class="line">cin &gt;&gt; x &gt;&gt; y;</span><br><span class="line">cout &lt;&lt; x + y &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Notes:</p><ul><li>the <code>&gt;&gt;</code> operator tries to interpret the input data according to the type of variable<ul><li>in this case, x as an <code>int</code>, so <code>&gt;&gt;</code> will expect to read chars that look like an <code>int</code> like <code>123</code> or <code>-42</code> and assemble those chars into a <strong>value</strong> </li><li>it will stop reading when it sees characters that is not part of an integer(letter or space)</li><li><code>cin &gt;&gt;</code>ignores leading whitespaces(spaces, tabs, newlines)</li><li>when reading from the keyboard, the program will pause waiting for the user input</li><li>pressing Enter causes the entered text to be submitted to the program (with \n)</li><li>Pressing ^D signals end of file, or end of input (without \n)</li></ul></li></ul><p> What if bad things happened?<br>    - input doesn’t contain an <code>int</code><br>    - input is too large&#x2F;small to fit in the variable<br>    - input is exhausted(not enough input&#x2F;data, i.e EOF) and get 2 int</p><p>The input operation fails, how can we test for this in our program?<br>    - if read failed(for whatever reason), <code>cin.fail()</code> return true<br>    - if EOF: <code>cin:eof()</code> will return true<br>    - But not until attempted read fails </p><p>Example: read all int from <code>stdin</code>, echo to <code>stdout</code>, one per line, stop at EOF or bad input</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">int main()&#123;</span><br><span class="line">int i;</span><br><span class="line">while(true)&#123;</span><br><span class="line">cin &gt;&gt; i;</span><br><span class="line">if(cin.fail()) break;  // checking for failure</span><br><span class="line">cout &lt;&lt; i &lt;&lt; endl; </span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">int main()&#123;</span><br><span class="line">int i;</span><br><span class="line">while(true)&#123;</span><br><span class="line">cin &gt;&gt; i;</span><br><span class="line">if(!cin) break; //cin variable is implicitly converted to a boolean value</span><br><span class="line">cout &lt;&lt; i &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>cin</code> converts to:</p><ul><li><code>true</code> if all good</li><li><code>false</code> if the stream has had a read failure</li></ul><p>Let’s take a closer look at &#x3D;&#x3D;<code>&gt;&gt; </code>operator:&#x3D;&#x3D;</p><ul><li><p><code>&gt;&gt;</code> is C’s (and c++’s) right bitshift operator</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">if a, b are ints, a &gt;&gt; b shifts a&#x27;s bits to the right by b spots</span><br><span class="line">Eg:</span><br><span class="line">21 &gt;&gt; 3</span><br><span class="line">= 2</span><br><span class="line">// 21 = 10101</span><br><span class="line">//2 = 10</span><br></pre></td></tr></table></figure></li><li><p>the <code>&gt;&gt;</code> operator has two operants </p><ul><li><code>cin</code> which is a stream</li><li>a variable to receive the input data , in our example;</li></ul></li><li><p>and it returns a result:</p><ul><li><code>cin</code> the same stream used as the first operand</li><li>it also has side effect</li></ul></li><li><p>the fact that it returns a stream, is why we can chain a series of those together</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">cin &gt;&gt; x &gt;&gt; y &gt;&gt; z</span><br><span class="line">[cin &gt;&gt; x] -&gt; cin</span><br><span class="line">  = cin &gt;&gt; y &gt;&gt; z</span><br><span class="line">   [cin &gt;&gt; y] -&gt; cin</span><br><span class="line">      = cin &gt;&gt; z</span><br></pre></td></tr></table></figure></li></ul>]]></content>
      
      
      <categories>
          
          <category> UW </category>
          
      </categories>
      
      
        <tags>
            
            <tag> course notes-24Fall </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
